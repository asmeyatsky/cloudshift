apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.api.secretName }}
  labels:
    {{- include "cloudshift.labels" . | nindent 4 }}
type: Opaque
data: {}
# Populate via:
#   kubectl create secret generic cloudshift-secrets --from-literal=api-key=YOUR_KEY
# Or use External Secrets Operator / Sealed Secrets in production.
